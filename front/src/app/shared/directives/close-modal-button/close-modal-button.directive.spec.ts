import { CloseModalButtonDirective } from './close-modal-button.directive';

describe('CloseModalButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseModalButtonDirective();
    expect(directive).toBeTruthy();
  });
});
